import 'package:flutter/material.dart';

class Populars extends StatelessWidget {
  const Populars({super.key});

  static const String routeName = '/popular';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
