enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, deliverd }

enum PaymentMethods {
  paypal,
  googlepay,
  applyPay,
  visa,
  materCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}

enum UpdateUserAction {
  displayName,
  email,
  password,
  bio,
  profilePic,
}

enum ConnectionType {
  wifi,
  mobile,
}

enum ErrorType {
  logoutErorr,
}

enum SuccessType {
  logoutSuccess,
  loginSuccess,
}
